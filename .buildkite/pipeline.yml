steps:
  - label: ":sparkles: Analyze changes"
    command: |
      npm install
      npm install --workspaces
      npm run pipeline
      cat apps/pipeline/dist/pipeline.yml | buildkite-agent pipeline upload
    plugins:
      - secrets#v1.0.0:
          variables:
            ANTHROPIC_API_KEY: ANTHROPIC_API_KEY
